========================================================================
    CONSOLE APPLICATION : AStart Project Overview
========================================================================

The program simply loads in hard coded terrain information
and uses A* to find the shortest path.  The algorithm is
can be used for any arbitrary searches.
